# Mmn 14

This project is meant to be compiled on x86-64 bit *unix environments.

Compilation is achieved via `cmake`:

```
mkdir build
cd build
cmake ../
```

gcc\clang compilation should easy as well, just make sure to include the `include` directory (-I flag).